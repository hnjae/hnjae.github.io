#!/usr/bin/env sh

# not-in-use 2022-08-15

ad="/usr/bin/asciidoctor"

$ad --trace --verbose \
  --attribute "skip-front-matter" \
  -
  # --base-dir ./content \                        #
  # --no-header-footer \                          #
  # --attribute nofooter \
  # --attribute docinfo=shared \
  # --attribute source-highlighter=highlightjs \  #
  # --attribute sectlinks \                       #
  # --attribute sectanchors \
  # --attribute figure-caption! \
  # --attribute toc-title! \
